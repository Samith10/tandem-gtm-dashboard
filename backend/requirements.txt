# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlmodel==0.0.21
httpx==0.27.2

# LLM providers -- install only what you need
# All four are listed so the image is self-contained for deployment
anthropic==0.40.0
openai==1.54.0
groq==0.11.0
google-generativeai==0.8.3

# Prod server extras (included via uvicorn[standard])
# websockets, httptools, uvloop, watchfiles

python-dotenv==1.0.1
